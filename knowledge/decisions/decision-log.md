# Decision Log

Only decisions that are **established** (implemented and verified) are logged here.
Proposals are not entered as decisions. Evidence for each decision lives in the
linked reports/source.

| # | Decision | Status | Evidence |
|---|---|---|---|
| 1 | CERT **r4.2** is the fixed dataset; no silent substitution of another release | established | [[../reports/foundation_report]]; `.opencode/instructions/ism-principles.md` |
| 2 | **user x day** is the primary analytical unit | established | leakage analysis, foundation report; `src/data/aggregation.py` |
| 3 | **Chronological TRAIN -> CALIBRATION -> TEST** split; random splits rejected for temporal behavior | established | `src/preprocessing/splits.py`, [[../docs/leakage_analysis]] |
| 4 | **LightGBM** is the current main model (lr 0.03, num_leaves 31, min_data_in_leaf 100, feature_fraction 0.8, bagging 0.8/freq 1, seed 42, up to 3000 rounds, early stop on CALIBRATION auc) | established | [[../reports/baseline_report]], [[../reports/phase4_report]] |
| 5 | **`lgbm-baseline-v2`** (8 features) is the frozen behavioral baseline; regression discipline applies to everything after it | established (Phase 4 freeze) | [[../reports/phase4_report]] section 7 |
| 6 | **TEST is never used for tuning**; thresholds and early stopping from CALIBRATION only; TEST evaluated once per finalized experiment | established | ism-principles; all reports |
| 7 | `scale_pos_weight` is computed **from TRAIN only** (255.66) | established | [[../reports/baseline_report]] |
| 8 | **Raw data never leaves Kaggle**; PC holds source/tests/docs/reports/knowledge | established | ism-principles section 9 |
| 9 | `unusual_access_count` uses a frozen **28-day strictly-past window** (robust to 14-42) | established (Phase 4) | [[../reports/phase4_report]] section 5.2 |
| 10 | `sensitive_file_access_count` defined **a priori from event-time magic bytes** (OLE2/PDF/ZIP), never from the answer key | established (Phase 3) | [[../reports/phase3_report]], `src/config.py` |
| 11 | Graph features are **experimental only**; not merged into the behavioral model; no accuracy claim | established (Phase 5) | [[../reports/phase5_graph_report]] |
| 12 | **External Kaggle notebooks are research material, not project dependencies**; they are not imported into the project and do not automatically become project knowledge | established | [[research/README]] |
| 13 | **`lgbm-graph-v1`** (12 features: 8 behavioral + 4 graph; `department_file_type_mismatch_count` REJECTED) is the frozen candidate baseline | established (Phase 7 freeze) | [[../reports/phase7_freeze_report]] |
| 14 | **Adaptive risk layer is NOT adopted**; `lgbm-graph-v1` remains the production candidate. Learned weights collapsed to ML-only ([1,0,0]) on CALIBRATION; the equal-weight control degraded CALIBRATION AUC-PR by 0.333 with doubled alert volume; graph-consistency "trust" semantics do not generalize to the label on this data | established (Phase 8) | [[../reports/phase8_adaptive_risk_report]] |
| 15 | **Production alert-prioritization policy = the frozen max-F1 threshold of `lgbm-graph-v1` (t = 0.9186015432508062)**. Selected by the documented CALIBRATION-only rule (threshold-identifiability S1, window alert-rate stability S2, window-coverage S3, then max CALIBRATION F1): CI width 0.0642 <= 0.2, no window outside [0.5, 2.0]x rate, no zero-recall window, CAL F1 0.589 vs 0.570 (best percentile) and 0.511 (best capacity). Capacity/percentile/50p families are documented candidates, not adopted: tight percentiles are temporally unstable (zero-alert window), high capacities collapse precision, the 50p point is degenerate (1 CAL alert). TEST once: 49 alerts, F1 0.354 — identical to the Phase 7 max-F1 point | established (Phase 9 freeze) | [[../reports/phase9_alert_prioritization_report]] |
| 16 | **Mondrian (label-conditional) split-conformal layer ACCEPTED as a diagnostic overlay on the frozen system** (Phase 10). Calibration = CALIBRATION block only (n1 323 / n0 58,677); alphas 0.01/0.05/0.10/0.20; tie-inclusive p-values; sets {0},{1},{0,1},empty. TEST (once): positive coverage 1.000 (Wilson 90% LB 0.917), negative 0.988; all 49 alerts confident-positive {1}; monitor band (scores in (0.4635, 0.9186)) enriches precision ~11x CAL / ~25x TEST. D1-D5 rule -> ACCEPT (D3 via monitor branch; uncertain-alert branch never fires). The layer reports honest class-conditional coverage and a watch-list; it does not change the alert policy. Caveat: class-1 is included for ~91% of rows (t1 = 0.0047) because the frozen model ranks many positives near zero | established (Phase 10) | [[../reports/phase10_conformal_report]] |
| 17 | **Explainability layer ACCEPTED as the reporting format for the frozen system** (Phase 11): LightGBM native `pred_contrib` (path-dependent TreeSHAP) in margin space; `margin = bias + Σ contrib` exact (max err ~4e-14, tol 1e-6); deterministic and bit-reproducible; shap TreeExplainer used only as a validation cross-check (max diff 0.0). Selection policy is label-free (alerts / monitor t0<=s<thr / borderline within 0.05 / decile sample); reasons are deterministic non-causal templates (no causal verbs). **Counterfactual explanations NOT SUPPORTED** (a counterfactual reports a hypothetical score the frozen model never produced; breaks the frozen-output contract; future phase allowed only under the recorded criteria). Frozen model/threshold/conformal overlay untouched (gates re-verified; TEST once: 49 alerts, AUC-ROC 0.93916, AUC-PR 0.26778 identical to records). Alert drivers (CALIBRATION 244 alerts / TEST 49): `usb_connection_count` (top-1 50% / 36-of-49), `device_consistency_score` (top-3 72%), `http_activity_count` bipolar (48.8% |share|, signed alert mean ≈ 0) | established (Phase 11) | [[../reports/phase11_explainability_report]] |
| 18 | **Operational envelope: RETAIN the frozen P0 alert policy** (Phase 12). No operational candidate (P1–P4: 1/3/7-day per-user de-duplication; rolling calibration threshold wd 7/14/30) satisfies the recorded CALIBRATION-only rule: R1 recall retention >= 0.90xP0 (de-duplication halves recall: 0.248/0.139 vs 0.517; rolling best 0.4613 vs bar 0.4653 — misses by 0.004), R2 alert reduction >= 10%, R3 Gini within +0.05, R4 zero-alert windows. `eligible = []` -> frozen threshold + no de-duplication stands. Capacity at 10,000 users is an INFERENCE projection (2,440 alerts/59 d under uniform scaling; clean-population bound is an untested HYPOTHESIS). TEST once (P0): 49 alerts, F1 0.354 — identical to the Phase 7/9 record (policy unchanged). Also fixed `cal_windows` (chunked rows instead of unique days: 8,429 vs 9 windows at wd=7 -> hours of CPU; now 1.06 s, regression-tested) | established (Phase 12) | [[../reports/phase12_operational_envelope_report]] |
| 19 | **Temporal stability of the frozen system evaluated; CAL verdict FAIL — documented risk, system unchanged** (Phase 13). Pre-registered protocol (`docs/phase13_specification.md`): 16-window 7-day grid over all 106 model-unseen days (CAL 59 + TEST 47), bounds B1–B6 from published records, verdict CAL-only (test-enforced), TEST as descriptive annex (frozen parquet re-read, never re-scored). Results (OBSERVED): 8/8 full 7-day CAL windows pass every bound (alerts/day 2.857–5.429 vs [2.068, 8.271]; precision 0.500–0.806 vs floor 0.342; recall 0.400–0.592 vs floor 0.25); the 3-day tail W8 (03-29..03-31) violates B5 hard (median 0.04141 vs band [0.05798, 0.23191]; 28.6% beyond the 10% margin) and B4 marginally (precision 0.3333) -> pre-registered rule yields **FAIL** (any violation beyond 10% margin). 15-day cross-check clean (B5/B6 NOT VERIFIED there). TEST annex 2/7 consistent (late low-positive windows, precision 0). Verdict is a risk-register statement — the frozen system stands unchanged; risk entries R13-1..R13-5 recorded in the master report Section 21. A strict replication window is infeasible inside r4.2 (data ends 2011-05-17; requires a new dataset/release). Deterministic local run (42 s, bit-identical rerun); no Kaggle execution needed; full suite 312 passed / 4 skipped | established (Phase 13) | [[../reports/phase13_temporal_stability_report]], [[../docs/phase13_specification]] |
| 20 | **User-level holdout generalization evaluated; evaluation-only; frozen system unchanged** (Phase 14). Pre-registered protocol (`docs/phase14_specification.md`): user-disjoint allocation (malicious: round-robin period 14, positions 0–7 TRAIN / 8–10 CAL / 11–13 TEST; benign: period 10, 0–7/8/9) asserted verbatim against the spec Section 6 table; blocks TRAIN 784 / 392,784 / 1,037 / 40 mal, CAL 108 / 54,108 / 379 / 15 mal, TEST 108 / 54,108 / 476 / 15 mal, pairwise disjoint; label reproduction 1,892 / 70; CDE1846 absent from the 1,000-user population entirely (0 rows; benign by exclusion). Auxiliary `lgbm-graph-v1-uhold` (12 frozen features, seed 42, scale_pos_weight 377.77 from user-disjoint TRAIN, early stopping on user-disjoint CAL AUC, best_iteration 214). Results (OBSERVED, auxiliary TEST once): AUC-ROC 0.786513 (band "materially worse" vs 0.939157), AUC-PR 0.375740 (+40.3% rel.; band "inconclusive"; cohort-conditional, not comparable); at the frozen threshold 0.9186015432508062: 1,026 alerts, precision 0.2018, recall 0.4349, F1 0.2756; P@10/30/50 = 1.0; coverage 13/15 malicious users (Wilson 90% [0.688, 0.933] → band "high"); user-level AUC 0.9649; user-block bootstrap AUC-ROC CI [0.666, 0.901]; determinism gate bit-identical (run1 32.96 s / run2 32.67 s). **NO production decision**; entity-level transport loss now measured (risk R14-1); `phase14_scenario.json` field `n_users` documented as holding stratum row counts (master report Section 26 #7, cosmetic). Full suite 346 passed / 4 skipped | established (Phase 14, evaluation-only) | [[../reports/phase14_user_holdout_report]], [[../docs/phase14_specification]] |
| 21 | **Unseen-user degradation diagnosed; evaluation-only; frozen system unchanged** (Phase 15). Pre-registered protocol (docs/phase15_specification.md); fully local run on frozen artifacts (no Kaggle, no prediction regeneration; chronological TEST never read). Gates: local TEST-score reproduction max diff 5.551e-17 (tol 1e-12), CAL AUC reproduced exactly (0.735512422820), double-run bit-identical. Findings (OBSERVED, labels as stated): (A) detection is incident-length dependent - 2-5 days 3/3, 6-12 3/4, 45-100 7/8; the two misses (JJM0203, WDD0366) hold 56 missed malicious days and produce 0 alerts in 501 days; 84.9% of alerts concentrate on the 8 long-incident users. (B) feature shift TRAIN->CAL->TEST is small on all 12 frozen features (|d| <= 0.179, PSI <= 0.026, KS <= 0.047) - not a dominant driver (INFERENCE). (C) missed users have long histories before onset (170/289 active days) - cold start excluded (INFERENCE); their max scores 0.498/0.726 vs detected median 0.999 - systematic ranking failures. (D) graph features are weak discriminators in BOTH blocks (row AUC 0.50-0.69; rare counts ~99.9% zero) - weakness is intrinsic to the feature set, not unseen-specific (INFERENCE); weak negative activity-consistency-stability link (r=-0.225, p=0.019). (E) no temporal-position effect (early 0.875 vs late 0.857). (F) alert explosion is an absolute-score calibration failure: 79.8% of 1,026 false positives lie within 0.10 above the frozen threshold; median FP margin 0.045; unseen-user benign rows inflate just above the boundary; the two misses are far below it (not threshold-edge). (G) coverage improves with unseen users (13/15 vs 5/5 chronological) at 12-21x alert cost; attribution CAN/CANNOT statements recorded. **NO production change** (consistent with decision #20); risks R15-1..R15-4 recorded. Full suite 379 passed / 4 skipped | established (Phase 15, diagnosis only) | [[../reports/phase15_unseen_user_diagnosis_report]], [[../docs/phase15_specification]] |
| 22 | **Final packaging & reproducibility verification completed; no ML** (Phase 16). The frozen system is packaged for external handoff: `README.md` (frozen system, pipeline, split, features, overlays, phases 12-15 results, risks, TEST-once, commands, research-not-production disclaimer), `requirements.txt` + `docs/environment.md` (PC env versions OBSERVED 2026-08-17; Kaggle env from policy doc + Phase 7 freeze record, unpinned versions documented as unrecorded), verification-only gate `scripts/verify_project.py` (37/37 checks PASS: structure; phase14/15 manifests md5+size 20/20; frozen model parse-only 186 trees/12 features/651,047 B; threshold exact 0.9186015432508062; all JSON/parquet load; master sections 0/4-30 + links; README links; seven credential patterns clean; full pytest suite 379 passed / 4 skipped; fresh baseline md5s for all artifacts recorded), `reports/artifacts/ARTIFACT_INDEX.md`, `final_verification_report.json`, `reports/phase16_final_packaging_report.md`. **No training/scoring/calibration; authoritative chronological TEST never re-scored; no artifact or historical report modified; frozen system unchanged.** Acceptance gates G1-G17 all PASS. The "single end-to-end reproducibility" item is closed as a verification-only gate (a full ML rerun would violate TEST-once) | established (Phase 16, packaging only) | [[../reports/phase16_final_packaging_report]], [[../README]] |
| 23 | **Calibration transfer evaluated; mechanical verdict FAIL; research-only; frozen system unchanged** (Phase 17). Pre-registered protocol (`docs/phase17_specification.md`); fully local run on frozen artifacts (no Kaggle, no prediction regeneration; authoritative chronological TEST never read). Gates 1/1b/2/3/4 all PASS; determinism 3× bit-identical (~218 s per run). Results (OBSERVED, user-disjoint TEST once, 54,108 rows / 476 pos): arm B (Platt, fitted on user-disjoint CAL 108 users / 379 pos) transfers calibration — TEST Brier 0.007962 (+92.25% vs raw 0.1027), ECE 0.002588, and its operating point recovers the recorded Phase 14 secondary record exactly (191 alerts, F1 0.4438, precision 0.7749, raw-space threshold 0.9837865316173778); transfer robust to CAL refit variance (bootstrap Brier CI [0.00787, 0.00849]). Arm C (binning+PAV): Brier 0.008669 (+91.56%) but operating point unusable (21,127 alerts, precision 0.0182; 3 distinct p-hat values on TEST). Arm D (ECDF): percentile map, not calibration — Brier 0.3247 (−216%), ECE 0.481. Verdict FAIL per pre-registered §14 criterion 3: \|ΔAUC-ROC\| > 1e-9 (C −0.0725, D −3.27e-6) — the empirical rank-based AUC estimator is not invariant under tie creation even when row order is preserved (spec §10.1/§12.2 "arm C preserves AUC-ROC" claim REFUTED by measurement); D also exceeds the 1e-12 strictly-monotone bound. Calibration cannot repair ranking failures (JJM0203/WDD0366 zero-alert under every conservative set; B/D coverage 10/15, C 15/15 only by alerting 39% of all rows). Spec §25: **FAIL ⇒ no Phase 18 calibration work on this mechanism without separate authorization**; no production change; risks R17-1..R17-5 recorded. One documented gate-4 correction: `final_verification_report.json` self-baseline excluded by owner decision (by-construction stale — verify_project.py hashes its own output before writing it; master report §26 #9). Full suite 407 passed / 4 skipped; verify_project 37/37 PASS | established (Phase 17, evaluation only) | [[../reports/phase17_calibration_transfer_report]], [[../docs/phase17_specification]] |
| 24 | **Phase execution transport = one phase bundle ZIP per phase; PC authoritative, Kaggle execution-only** (2026-08-20). No bulk per-file sync during normal execution. In: `phase<PHASE>_kaggle_bundle.zip` (user uploads to `/kaggle/working/`; per-file manifest md5/size + metadata zip md5, verified on both sides; extracted to `/kaggle/working/ism_project/`; ZIP kept until phase completes). Out: `phase<PHASE>_results_bundle.zip` (artifacts, manifest, metrics, status/log, checkpoints, report, tests summary; preserved back to PC after integrity verification). Previous phases consumed as frozen verified artifacts (never rerun); CERT r4.2 structurally verified under `/kaggle/input/`; CPU default; `phase<PHASE>_live.log` / `_status.json` / `_checkpoints/` runtime state; checkpoints after every important stage; `pretest_freeze.json` / `test_opened` / `test_completed` / `test_complete.json` TEST-once discipline unchanged; failure handling per policy section 0.5 (before/during/after TEST); pre-registered PASS/FAIL only; priority correctness > leakage safety > reproducibility > valid evaluation > interpretability > compute efficiency > implementation simplicity. Scientific specs remain authoritative: on conflict with this workflow, preserve the spec, STOP, report | established (2026-08-20) | [[../docs/kaggle-execution-policy]] section 0 |
| 25 | **Phase 18 pre-TEST execution amendment: implementation-only runtime optimization + determinism alignment to the spec double run** (2026-08-20, authorized; SCIENTIFIC CHANGE: none). OBSERVED: `r_context_block` is the entire Phase 18 runtime bottleneck (cProfile on a 60-user TRAIN subset: 139,904 np.median calls = 80% of runtime; serial extrapolates to ~7,700 s/TRAIN, matching the 7,992-s run 1 of the aborted first execution). CHANGE (implementation only): precomputed user->row index maps replace per-pool `np.isin` scans; independent peer-pool computations run in parallel processes (`workers` param, default min(4, cpu_count)); `np.median` replaced by `_median_axis0` (identical element selection/float arithmetic for finite inputs, falls back to np.median otherwise); frozen serial reference kept as `_r_context_block_serial` for tests. Benchmark (OBSERVED, PC, bit-identical everywhere): TRAIN 256 users (128,256 rows) serial 1997 s vs optimized w4 229 s (8.7x); CAL full (54,108 rows) serial 58 s vs w4 42 s; synthetic + edge-case fixtures bit-identical (tests A-H). DETERMINISM: gate 7 aligned to spec Section 17.7 (full pipeline run exactly twice; `runs_required=2`, `runs_completed=2`, `determinism_ok` carried into run 2's mechanical verdict, phase14/17 convention); the original plan's third engine-level run was a phase17 precedent, not spec text, and is removed; n=1000 bootstrap unchanged. TEST STATUS: test_opened=false, test_completed=false; authoritative TEST never read during this amendment. Recorded in `run_phase18.py` `execution_amendment` (experiment artifact) and tests test_phase18.py | established (2026-08-20) | [[../docs/phase18_specification]] sections 15/17.7/21; `src/experiments/phase18.py`; `kaggle_scripts/run_phase18.py`; `kaggle_scripts/benchmark_phase18_context.py` |
| 26 | **Phase 19 end-to-end CONFIRM evaluation completed; verdict FAIL; adaptive risk rejected for production; frozen system unchanged** (2026-09-11). One-time authorized evaluation of adaptive risk A0 vs Family C on the CONFIRM partition (196 users, 98,196 rows, 245 positives, overlap=0). Thresholds from DEV_OOF (A0=0.989445, C=0.358667). No training, no threshold tuning. Results (OBSERVED): A0 — ROC-AUC 0.7789, PR-AUC 0.2984, F1 0.3407, alerts 72, precision 0.7500, recall 0.2204, MCC 0.4059, balanced accuracy 0.6101. Family C — ROC-AUC 0.7507, PR-AUC 0.2971, F1 0.3386, alerts 74, precision 0.7297, recall 0.2204, MCC 0.4003, balanced accuracy 0.6101. Deltas: ROC-AUC -0.0281, PR-AUC -0.0013, F1 -0.0021. Verdict FAIL (delta ROC-AUC -0.0281 < -0.01 threshold). Adaptive risk component definitions rejected; new definitions would require a new authorized phase. Frozen system unchanged since Phase 7/9. No ML phase running or scheduled. Full suite 80/0 tests passed | established (Phase 19, evaluation only, no adoption) | [[phase19_v1_1_1/phase19.py]]; `phase19_v1_1_1/phase19_v1_1_2_confirm_result.json`; `phase19_v1_1_1/run_phase19_confirm_once_v1_1_2.py` |
| 27 | **Phase 20 full-scale operational validation completed; complete decision table materialized; no ML change; frozen system unchanged** (2026-09-12). Complete 501,000-row decision table generated from frozen components using `scripts/phase20_materialize_decisions.py`. Frozen LightGBM model (lgbm-graph-v1) scored all 501K user-days; risk levels computed (ALERT=3785, BORDERLINE=1484, MONITOR=178204, NON-ALERT=317527); conformal confidence integrated; SHAP explanations for 21043 TEST-alert rows with feature-value fallback for remainder; trust diagnostics for 476200 rows. Score integrity: 0 mismatches vs Phase 7 predictions. Determinism: PASS (MD5 match). Leakage audit: PASS. 54/54 tests pass (39 unit + 15 full-table integration). Runtime 180s, peak memory 1.7GB. Parquet MD5=6476f791d1cc9f21327a94e1373f9e09. No ML change; frozen system unchanged. | established (Phase 20, full-scale validation, no ML change) | [[../reports/artifacts/phase20/PHASE20_REPORT.md]]; `reports/artifacts/phase20/final_user_day_decisions.parquet`; `tests/test_phase20.py` |


## Related

- [[experiments/experiment-index]]
- [[features/feature-registry]]
- [[../docs/leakage_analysis]]
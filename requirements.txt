# ISM Project Shafe — portable dependencies
# Tested on Python 3.11+ (Windows, Linux, Kaggle)

numpy
pandas
lightgbm
scikit-learn
scipy
pyarrow
pytest

# Notebook dependencies (optional for core tests)
matplotlib
PyYAML

# Kaggle-only / optional (not needed for core tests or frozen-artifact tests)
# duckdb        — used only in Kaggle streaming aggregation paths
# torch         — used only in Phase 19 families H/I/J (try/except guarded)
# torch_geometric — used only in Phase 19 family J feasibility check
